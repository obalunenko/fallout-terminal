# Validation: Player Sessions and Shared Terminal Control

> **SUPERSEDED LEGACY PLAYER TRANSPORT — HISTORICAL, NON-AUTHORITATIVE.**
> Any WebSocket or handwritten JSON player-transport description in this retained
> completed feature document has been replaced by the generated ConnectRPC contract in
> [`specs/005-connectrpc-protobuf-migration/contracts/public-player.md`](../005-connectrpc-protobuf-migration/contracts/public-player.md).

Validated on 2026-08-12 from the feature working tree.

## Automated gates

| Gate | Result |
|---|---|
| `gofmt -l .` | PASS — no files reported |
| `go vet ./...` | PASS |
| `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1` | PASS — all Go packages, including `internal/playerconfig` |
| `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1` | PASS — all Go packages after reducing unrelated large-payload serialization in the slow-observer fixture; the production queue behavior was unchanged |
| `npm --prefix frontend run build` | PASS — Vite production bundle generated |
| `npm --prefix tests/browser test` | PASS — 30/30 Playwright journeys |

The browser fixture intentionally returns 404 for optional sound endpoints; tests use the static asset server and those responses are expected. Playwright also reports that `NO_COLOR` is ignored because `FORCE_COLOR` is set; this does not affect results.

## Success-criteria evidence

| Criterion | Evidence |
|---|---|
| SC-001 | `internal/control/service_test.go`: 100-way same-character claim races produce one claimant. |
| SC-002 | `internal/control/service_test.go`: concurrent first assignments establish exactly one controller. |
| SC-003 | `tests/browser/player-sessions-control.spec.mjs` and `internal/player/server_test.go`: reload, reopen, and three-tab recognition journeys. |
| SC-004 | Same files: recognized reconnect restores assignment without selection. |
| SC-005 | Browser-context, cleared-storage, stale-token, and fresh-process tests. |
| SC-006 | `internal/control/service_test.go`: disconnect retains claims until explicit release/transfer. |
| SC-007 | Control and Playwright observer tests cover navigation, guess, pattern, paging, hover, focus, and back with zero canonical mutation. |
| SC-008 | `internal/control/service_test.go`: active, observer, unassigned, disconnected, stale, unknown, and expired authorization outcomes. |
| SC-009 | Multi-tab final-close tests prove zero automatic promotion. |
| SC-010 | Reconnect-before and reconnect-after-reassignment control/server/browser tests. |
| SC-011 | 100 reassignment/action interleavings plus role-only browser convergence. |
| SC-012 | Roster correction tests compare terminal/private puzzle state before and after every correction. |
| SC-013 | `internal/control/service_test.go`: 100 forced action/reassignment interleavings. |
| SC-014 | Real-socket 4–7 client convergence, one-use action, and slow-observer isolation tests. |
| SC-015 | US7 real-socket and browser journeys follow ten terminal switches and a late assignment. |
| SC-016 | US8 live tests deep-compare private checkpoints; socket/browser tests compare restored public puzzles. |
| SC-017 | Preserve, discard, and cancel tests across live, control, server, app, and browser surfaces. |
| SC-018 | US9 control/server tests clear the broadcast epoch while retaining recognized sessions and roster. |
| SC-019 | Second-broadcast reselection and fresh-process unknown-token replacement tests. |
| SC-020 | `internal/domain/model_test.go`: byte-identical version-1 encoding across complete runtime activity. |
| SC-021 | `internal/platform/assets_test.go`, strict protocol decoding, and private Wails bridge tests prove no player `ForceHackSuccess` path. |
| SC-022 | Full Go and browser suites pass, including existing hacking generation, guesses, likeness, attempts, patterns, dud removal, lockout, content, and trusted success regressions. |
| SC-023 | `internal/playerconfig/service_test.go` creates a config, saves three stable ordered roster entries, and reloads them through both the relative session reference and shared-file open path. |
| SC-024 | `internal/domain/model_test.go` proves legacy version-1 sessions omit `playerConfig` and round-trip unchanged; the live master surface presents select/create while roster and broadcast controls remain disabled. |
| SC-025 | Player-config/domain/session/App tests cover cancel, missing/invalid data, unsupported versions, duplicate IDs, association failures, and coordinator write failures without partial installation or publication. BUG-002 regressions additionally distinguish a non-nil empty `[]` roster from nil through service result cloning, coordinator installation, session association, and the first durable roster add. |
| SC-026 | `internal/control/service_test.go` verifies successful add, rename, and unclaimed delete persist complete ordered candidates; injected save failure preserves the prior detached coordination snapshot and emits no effect. |
| SC-027 | `internal/domain/model_test.go` inspects exact session/player-config JSON field sets and rejects runtime identity, presence, claim, controller, broadcast, request, navigation, and puzzle fields. |
| SC-028 | `tests/browser/player-sessions-control.spec.mjs` runs the bundled browser against a real Go coordinator/player server: the active controller selects an ordinary filler, an unused special pattern, and a password candidate; each emits one exact request, mutates canonical state once, converges the observer, and resolves pending authoritatively, while observer clicks emit no shared action. |
| SC-029 | `tests/browser/player-sessions-control.spec.mjs` starts from the visible enabled game-master control, accepts the in-app confirmation, observes one authoritative `endBroadcast` call, applies the returned no-broadcast state, and hides the control. `internal/player/server_test.go` proves the matching coordinator transition publishes one revision of `PLAYER_STATE` plus `TERMINAL_CLEAR` to both active and observer players while retaining logical-session identities and roster entries. The production `.app` click-through additionally confirms that a real connected player immediately leaves the active terminal and returns to the unassigned waiting state. |
| SC-030 | BUG-005 live/control/App/player/asset/browser/native tests prove one private reset of the current failed active puzzle creates one fresh full-attempt generation for all assigned players while preserving broadcast, terminal, session, assignment, controller, roster, other runtime, and durable state. |
| SC-031 | Full Go, race, frontend, 30-case browser, Wails package, and native journeys pass with lockout, unfinished-puzzle preservation, broadcast lifetime, prior bugfix, and player-authority boundaries intact. |

## macOS development and package smoke gate

- `wails dev`: PASS for generation, frontend startup, development compilation, self-signing, native launch, and graceful Ctrl+C shutdown. The master start surface rendered correctly in the native window. The environment presented an unrelated Chrome local-network permission sheet while attempting deeper coordinate-driven interaction, so no claim is made for native automation beyond launch/render; the affected master/player state journeys are covered by the 24 Playwright cases and real-socket Go integration suite above.
- `wails build -clean`: PASS for bindings, frontend compilation, Go compilation, packaging, self-signing, and the Darwin post-build hook. Output: `build/bin/Fallout Terminal.app` for `darwin/arm64`.
- The Wails development build reported its expected private-API warning and is for development testing only. Distribution signing identity, hardened-runtime entitlements, Apple notarization credentials, and App Store approval were not available or attempted; the successful package is locally self-signed.

## BUG-001 development and package rerun

- `wails dev`: PASS for binding generation, Vite startup, development compilation, packaging, and self-signing. A second launcher could not claim the already-running Wails bridge on `127.0.0.1:34115`; that existing instance was left untouched. Read-only browser inspection of the live master verified visible select/create player-config actions, `НЕ ВЫБРАНА` recovery status, disabled roster-add and broadcast-start controls, and no application-script error. The live player address on `127.0.0.1:3690` rendered the authoritative waiting state.
- `wails build`: PASS after the final BUG-001 changes for generated bindings, frontend and Go compilation, Darwin packaging, self-signing, and the post-build hook. Output remains `build/bin/Fallout Terminal.app` for `darwin/arm64`.
- Browser-console noise from the Wails bridge opened directly in Chrome and missing optional sound routes in the static Playwright fixture was inspected and treated as harness-only; neither appeared in the application code path or failed a test.

## BUG-002 empty-roster regression rerun

- Targeted red tests reproduced `roster must be an array` at both the player-config service result and the real App create/associate/install boundary before the fix. After preserving non-nil empty slices at both clone boundaries, the focused player-config, domain, coordinator, and App suites pass.
- `gofmt -l .`, `go vet ./...`, `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1`, and `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1`: PASS. The networked Go suites required normal loopback bind access after the restricted sandbox rejected local listeners.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 25/25 journeys, including the BUG-002 master contract that treats player-config metadata, not roster length, as the active-config boundary. Optional sound-route 404s and the `NO_COLOR`/`FORCE_COLOR` warning remain expected fixture noise.
- Real `wails dev` native journey: PASS. The game master created `/private/tmp/.../session.json`, then a new `players.json` containing exactly `"roster": []`. The master activated it without `roster must be an array`, showed no player-config or coordination error, enabled add/broadcast controls despite the empty roster, and persisted the first added character to the same file. The session stored the relative association `"playerConfig": "players.json"`.
- Direct Chrome inspection of the Wails browser-development bridge logged the known `wails/ipc.js` DOM-runtime error and browser-extension asynchronous-channel errors. Bound Go commands, native dialogs, authoritative UI state, and file writes completed successfully, so this noise is treated as development-browser harness-only.
- `/Users/olegbalunenko/go/bin/wails build`: PASS for bindings, dependency installation, frontend and Go compilation, Darwin packaging, self-signing, and the post-build hook. Output: `build/bin/Fallout Terminal.app` for `darwin/arm64`.

## BUG-003 active-controller hacking regression

- The production-composed browser regression first reproduced the report at the client boundary: a real generated puzzle encoded its empty activity log as `null`; clicking a target entered pending state, but `renderHackLog()` threw on `hack.log.map(...)` before the outbound send.
- The correction preserves a non-nil empty hacking log through private/public clone boundaries and defensively treats a null or malformed browser log as empty. The real composed journey now passes for filler, unused-pattern, and password selection with exact request preconditions, one accepted mutation per click, converged active/observer surfaces, authoritative pending completion, and zero observer hacking frames.
- `gofmt -l .`, `GOCACHE=/private/tmp/fallout-go-cache go vet ./...`, `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1`, and `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1`: PASS.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 27/27 journeys. The `NO_COLOR`/`FORCE_COLOR` notice remains benign.
- `/Users/olegbalunenko/go/bin/wails dev`: PASS for binding generation, Vite startup, native development compilation, packaging, self-signing, launch, and graceful shutdown. Direct Chrome inspection of the development bridge again emitted the known Wails `ipc.js` DOM-runtime noise; no native file-dialog automation was claimed because macOS assistive access was unavailable.
- `/Users/olegbalunenko/go/bin/wails build`: PASS for bindings, frontend and Go compilation, Darwin packaging, self-signing, and the post-build hook. Output: `build/bin/Fallout Terminal.app` for `darwin/arm64`.

## BUG-004 game-master end-broadcast regression

- The production-shaped master regression first reproduced the failure at the confirmation boundary: `#btnEndBroadcast` was visible and enabled, but the only confirmation was native `window.confirm`; no controllable in-app confirmation existed and `EndBroadcast` was never reached when that native gate was unavailable.
- The correction replaces that gate with an accessible modal `<dialog>`, disables both confirmation actions synchronously to prevent duplicate commands, awaits the authoritative result, requires the returned state to contain no broadcast, and keeps command failures visible through the existing coordination alert.
- Focused regression evidence passes: the master journey invokes `desktopAPI.endBroadcast()` exactly once and returns to the non-live presentation; the App lifecycle test invokes the coordinator once; the real player server publishes the same no-broadcast revision and `TERMINAL_CLEAR` to active and observer clients while retaining sessions, fallback names, and roster entries. Asset-contract coverage prevents reintroduction of the native confirmation dependency.
- `gofmt -l .`, `go vet ./...`, `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1`, and `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1`: PASS.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 29/29 journeys, including the new BUG-004 master action and the existing US9 player lifecycle journey. The `NO_COLOR`/`FORCE_COLOR` notice remains benign.
- `/Users/olegbalunenko/go/bin/wails build`: PASS for binding generation, frontend and Go compilation, Darwin packaging, self-signing, and the post-build hook. Output: `build/bin/Fallout Terminal.app` for `darwin/arm64`.
- Production `.app` native click-through: PASS when opened through Finder. The game master loaded `sessions/demo.json`, created and activated `/private/tmp/fallout-bug004-native-players.json`, added `Mara`, started a broadcast, accepted the real Chrome player connection at `http://127.0.0.1:3690/`, assigned the character, and made the selected terminal active. The visible `ЗАВЕРШИТЬ ТРАНСЛЯЦИЮ` control opened the in-app confirmation dialog; confirming it returned the master to `ТРАНСЛЯЦИЯ ЗАВЕРШЕНА`, restored the start control, retained `Mara` and logical session `PLAYER 1`, and immediately changed the connected player from the active terminal to `ОЖИДАНИЕ ТРАНСЛЯЦИИ` with no assignment. This completes the T138/T114 native gate.

## BUG-005 failed-hack retry regression

- Focused red coverage reproduced the missing boundary: live/control/App/player/asset tests did not compile without `ResetFailedHack`, and the production-shaped Playwright journey reached `ВЗЛОМ: ЗАБЛОКИРОВАН` but could not find `#btnResetFailedHack`.
- The correction adds one trusted `ResetFailedHack` command. The coordinator accepts it only for the still-current failed, unsolved active terminal, replaces only that runtime slot from the latest validated authored payload, assigns one revision, and publishes the complete fresh terminal projection through the existing ordered effect sinks. A 32-way concurrent duplicate test accepts exactly one reset and one revision; duplicate, stale-terminal, absent, unfinished, and solved calls retain their prior revision and state.
- Fresh candidate IDs now include a private generation-derived scope, matching the existing generation-bound pattern IDs. Delayed word and pattern actions from the failed generation therefore cannot address the replacement, even when row/column placement happens to repeat. Unknown guesses are rejected without canonical mutation.
- `gofmt -l .`, `GOCACHE=/private/tmp/fallout-go-cache go vet ./...`, `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1`, and `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1`: PASS.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 30/30 journeys, including BUG-005 four-attempt exhaustion, visible enabled master retry, exactly one command, active/observer convergence, stable broadcast/terminal/character/role identity, and no observer action authority. The `NO_COLOR`/`FORCE_COLOR` notice remains benign.
- `wails build`: PASS for generated bindings, frontend and Go compilation, Darwin packaging, self-signing, and the post-build hook. Output: `build/bin/Fallout Terminal.app` for `darwin/arm64`.
- Production `.app` native journey: PASS. The packaged master loaded `sessions/demo.json` with the temporary `/private/tmp/fallout-bug005-players.json`, started a clean broadcast, activated the level-1 terminal, and served two real headless Chromium player contexts at `http://127.0.0.1:3690/`. The first became active and the second observer. Four authoritative filler guesses converged both screens on blocked state; the master simultaneously displayed `ВЗЛОМ: ЗАБЛОКИРОВАН` and enabled `ПОВТОРИТЬ ВЗЛОМ`. One native click removed both blocked overlays, delivered a visibly different fresh board to both players, retained active/observer roles and the same broadcast/terminal, and returned the master status to `ВЗЛОМ: осталось попыток 4/4` without restarting the broadcast. Temporary mutation of `sessions/demo.json` was reverted after the journey.

| Criterion | BUG-005 evidence |
|---|---|
| SC-030 | Live/control tests inspect the private generation change, full attempts, empty log, latest level/content, unaffected runtime slots, stable coordinator identity/ownership fields, one revision, duplicate/stale refusal, and old-generation action rejection. App/asset tests keep the exact command private; player-server and browser/native journeys prove active/observer convergence without a player reset surface. |
| SC-031 | The complete Go, race, frontend, 30-case browser, Wails package, and native active/observer journeys pass while existing lockout, unfinished-puzzle preservation, terminal switching, broadcast lifetime, player-config, BUG-001–BUG-004, and `ForceHackSuccess` gates remain green. |

## BUG-006 authoritative hacking outcome audio regression

- Focused red coverage reproduced the report at the Web Audio eligibility boundary: the player document received its own gesture and the bad/good assets were loaded, but an authoritative wrong, solved, or lockout `HACK_STATE` produced zero `AudioContext` source starts. Cursor and click sounds remained independently observable.
- The correction primes and resumes each document's Web Audio context during its first user gesture and treats that document as eligible only after the context is running. Outcome playback remains a non-fatal side effect of the existing accepted-revision and previous-to-next hacking-state comparison; no click, `ACTION_RESULT`, WebSocket, canonical state, or pending-completion behavior changed.
- The sound adapter now offers an optional playback observer after a successful source start. Browser regressions use it to distinguish `hack-bad` and `hack-good` from cursor/click sounds without replacing the production Web Audio path.
- `gofmt -l .`, `GOCACHE=/private/tmp/fallout-go-cache go vet ./...`, `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1`, and `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1`: PASS.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 33/33 journeys. The first complete browser run recorded one randomized BUG-003 fixture no-change assertion after a special pattern removed a dud; the immediate complete rerun passed 33/33 without code changes. The `NO_COLOR`/`FORCE_COLOR` notice remains benign.
- `/Users/olegbalunenko/go/bin/wails build`: PASS for generated bindings, frontend and Go compilation, Darwin packaging, self-signing, and the post-build hook. Output: `build/bin/Fallout Terminal.app` for `darwin/arm64`.
- Packaged native player smoke: PASS. The built `.app` served non-empty `hack-bad` and `hack-good` manifests/assets from `127.0.0.1:3690`. Six fresh headless Chromium documents loaded those packaged assets: separately gestured active and observer documents for ordinary wrong, newly solved, and final lockout cases. Each document recorded exactly one successful source start from `hack-bad`, `hack-good`, and `hack-bad` respectively, and duplicate authoritative projections produced no replay.

| Criterion | BUG-006 evidence |
|---|---|
| FR-122 / FR-123 | Active and observer browser regressions plus the packaged native smoke apply separate fresh wrong, unlock, and lockout projections after a per-document gesture and observe exactly one applicable successful Web Audio source start. |
| FR-124 | Focused browser coverage records no outcome URL for local selection, `ACTION_RESULT` alone, rejection, initial/reconnect `TERMINAL_LIVE`, stale or duplicate revisions, or unchanged re-rendering. |
| FR-125 | Ineligible-context and simulated source-start failures remain silent while the authoritative attempts display updates and accepted pending input completes. Playback observation and failure handling submit no request and mutate no canonical state. |
| SC-032 | Three fresh active/observer cases pass in both the browser regression and packaged player smoke with per-document gestures and exact bad/good/bad playback; all specified no-op and failure paths remain silent. |

## BUG-007 complete hacking-screen audio reproduction

- The first sandboxed Core Audio inventory was empty, but the same read-only inventory outside the Codex sandbox found the default MacBook Air speakers and microphone plus the installed Elgato Wave Link endpoints. The packaged Wails player and production-composed fixture were therefore run outside the sandbox for native output checks.
- Direct listening in output-capable Chrome confirmed audible `ambient`, `charscroll`, `single`, and `multiple` controls after one explicit document gesture. Selecting one password word then produced a rapid preview-family burst instead of a clean, distinct entry/outcome sequence.
- A focused production-DOM regression localized the first broken boundary: one word click records `enter` followed by an unintended `multiple`. `beginSharedAction()` calls the full `render()`, which rebuilds `#hackColumns` under the stationary pointer; `ACTION_RESULT` and authoritative `HACK_STATE` repeat the same reconstruction. The replacement nodes synthesize another preview transition that overlaps entry and outcome feedback.
- Red command: `npx playwright test hacking-camouflage.spec.mjs --grep 'one word selection plays one enter cue'` — FAIL as expected with received families `["enter", "multiple"]`, wanted `["enter"]` before any authoritative result.
- The correction updates only player presentation: entering or resolving shared-input pending state now refreshes identity/authority classes without rebuilding the hacking board; authoritative board updates preserve the current hover target and restore its highlight without replaying its cue; repeated movement inside one unused pattern is deduplicated by stable pattern ID.
- Focused green command: `npx playwright test hacking-camouflage.spec.mjs --grep 'one word selection plays one enter cue|authoritative wrong-guess audio'` — PASS, 2/2. One word click now records exactly `enter`; its accepted wrong projection then records exactly `hack-bad`, with no intervening `single` or `multiple` replay.
- User-monitored output-capable Chrome initially confirmed audible `ambient`, `charscroll`, `single`, and `multiple`; the reported word click reproduced the unwanted rapid preview burst. After the render/deduplication repair, the same native journey confirmed one hover cue with no burst and one distinct `enter` cue.
- A native diagnostic trace then showed successful source starts for `multiple` and `enter` in a running production `AudioContext`, while the visible canonical puzzle advanced to lockout without any outcome invocation. The production coordinator publishes accepted gameplay through revisioned `TERMINAL_LIVE`; outcome comparison had existed only in the legacy/focused `HACK_STATE` branch.
- Outcome comparison now runs through one common transition function for both revisioned envelope families. Continuous same-terminal `TERMINAL_LIVE` updates compare prior and next public puzzles; first delivery, reconnect welcome, terminal switch, stale/duplicate state, and unchanged refreshes establish or retain baselines without replay. A solved live update retains the existing delayed transition to normal navigation.
- Folder semantics were confirmed as: `ambient` loop; `menu-focus` normal-menu hover; `single` hacking-symbol hover; `multiple` hacking-word or unused-pattern hover; `enter` hacking target click; `charscroll` each newly revealed text line; `hack-bad` authoritative wrong/lockout; `hack-good` authoritative unlock. Folder manifests and supported non-empty assets are asserted for every family.
- Web Audio enablement now waits for one shared warmup that loads and natively decodes all one-shot folders. Playback requests await that readiness promise, preventing first-use decode completion from reordering a hover cue across a subsequent click.
- Final user-monitored Chrome journey confirmed exactly one `multiple` hover cue, exactly one `enter` click cue, and exactly one authoritative `hack-bad` or `hack-good` outcome cue. Direct Core Audio playback separately confirmed both packaged `hack-bad` and `hack-good` assets are audible; automated production-envelope coverage verifies both mappings, exact-once active/observer playback, and all no-replay paths.
- The complete browser gate exposed one additional replacement-node edge: a late `mouseout`/`focusout` from a detached pre-update hacking cell could clear the preserved hover key and let the replacement cell replay `multiple`. Detached-cell exit events are now ignored; a focused ten-run stress command (`npx playwright test hacking-camouflage.spec.mjs --grep "one word selection" --repeat-each=10`) passes 10/10 with the exact `multiple` → `enter` → authoritative outcome sequence and no preview replay.
- Final implementation gates on the corrected tree: `gofmt -l .`, `GOCACHE=/private/tmp/fallout-go-cache go vet ./...`, `GOCACHE=/private/tmp/fallout-go-cache go test ./... -count=1`, `GOCACHE=/private/tmp/fallout-go-cache go test -race ./... -count=1`, `git diff --check`, and JavaScript syntax checks all PASS. `npm --prefix frontend run build` passes; `npm --prefix tests/browser test` passes 34/34; `/Users/olegbalunenko/go/bin/wails build` passes binding generation, frontend and Go compilation, Darwin/arm64 packaging, self-signing, and the post-build hook.

| Criterion | BUG-007 evidence |
|---|---|
| FR-126 / FR-127 | Production-shaped browser and directly monitored output-capable journeys map hacking symbols to one random `single`, words/unused patterns to one random `multiple`, and clicks to one random `enter`, with stable-target deduplication across pointer/focus and authoritative board replacement. |
| FR-128 | Both accepted envelope families use one previous-to-next transition function: wrong and final lockout play one `hack-bad`, newly solved plays one `hack-good`, while initial/reconnect, stale/duplicate, unchanged, rejected, and `ACTION_RESULT`-only paths remain silent. |
| FR-129 | One shared gesture-time readiness promise loads and natively decodes all one-shot folders before playback eligibility; playback failure remains non-fatal and cannot alter authority, canonical state, or pending completion. |
| SC-033 | The packaged player directly produced audible ambient/charscroll controls and the requested `1` hover, `2` enter, `3` authoritative outcome sequence. Full 34-case browser, Go/race/vet, frontend, and Wails package gates pass, plus the focused anti-replay case passes 10/10. |

## T157 post-convergence final validation

- `gofmt -l .`: PASS with no Go source paths. `GOCACHE=/private/tmp/fallout-t157-go-cache go vet ./...`: PASS.
- `GOCACHE=/private/tmp/fallout-t157-go-cache go test ./... -count=1`: PASS across every package after rerunning outside the restricted sandbox so the player and tunnel integration suites could bind their required loopback listeners. The first sandboxed attempt failed only with `bind: operation not permitted`.
- `GOCACHE=/private/tmp/fallout-t157-go-cache go test -race ./... -count=1`: PASS across every package with loopback access enabled.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 34/34 journeys. Playwright required loopback access for its fixture server; the `NO_COLOR`/`FORCE_COLOR` warning remained benign.
- `/Users/olegbalunenko/go/bin/wails dev`: PASS for dependency preparation, generated bindings, frontend and Go compilation, development packaging, self-signing, launch, an HTTP 200 response from the Wails development bridge, and graceful Ctrl+C shutdown. The expected development-only private-API/App Store warning was reported.
- Generated-binding inspection confirms `SetLiveTerminal` and `ClearLiveTerminal` are absent from the Wails bindings, desktop facade, and bound App surface, while `RequestTerminalActivation`, `RequestTerminalClear`, `UpdateLiveTerminal`, and private `ForceHackSuccess` remain available.
- `/Users/olegbalunenko/go/bin/wails build -clean`: PASS for generated bindings, frontend and Go compilation, clean Darwin/arm64 packaging, self-signing, and the post-build hook. Output: `build/bin/Fallout Terminal.app`.
- `git diff --check`: PASS. Distribution signing identity, hardened-runtime signing, notarization, stapling, and DMG release smoke remain unavailable release-only gates because release credentials and release packaging were not in scope; none is claimed as completed.

## BUG-008 authoritative controller presentation regression

- The player presentation is now canonical and controller-owned. Menu selection, page, hacking-cell/word/pattern highlight, and preview are carried by the generated ConnectRPC/protobuf snapshot. Observer pointer, focus, keyboard, paging, back, activation, and hacking-preview paths are inert before either local presentation mutation or an outbound request.
- Reassignment changes presentation authority atomically at the coordinator revision: the new controller continues from the unchanged shared presentation and can interact immediately; the former controller becomes an observer and follows later authoritative presentation updates. Delayed presentation requests are revalidated against the controller at processing time, and stale client results cannot overwrite a newer authoritative state.
- Protocol and generated-boundary gates pass: `scripts/proto-check.sh`, `scripts/proto-breaking.sh --all-fixtures`, `scripts/wails-bindings-check.sh`, and generated source/build validation. The schema revision is `89e5f5a7d0aadc0f5f62a5825787ae15e77e524f6cead55aa3ff15eaf7aefaa3`.
- `make fmt-check`, `go vet ./...`, and `GOCACHE=/private/tmp/fallout-bug008-go-cache go test ./... -count=1`: PASS across all Go packages. `FALLOUT_NGROK_*` integration overrides unset, `GOCACHE=/private/tmp/fallout-bug008-final-race2-cache go test -race ./... -count=1 -timeout=5m`: PASS across all packages outside the restricted sandbox so `httptest` could bind loopback ports; the real-ngrok integration remains correctly opt-in. The linker emitted only the existing macOS deployment-target warnings.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 125 passed, 2 explicitly opt-in real-ngrok cases skipped. The complete production-composed controller/observer matrix covers menu, pagination, hacking preview, rapid latest-wins presentation requests, role reassignment, late join, reload, reconnect, state-changing terminal transitions, prior audio behavior, responsive layout, and private-operation gates.
- `make package`: PASS for protobuf verification, generated client and overseer builds, Wails binding checks, dependency/license checks, clean Darwin/arm64 application compilation, packaging, and self-signing. Output: `build/bin/Fallout Terminal.app`. Direct `wails build -clean` is not a valid build entry point for this Wails v3 repository because it intentionally has no legacy `wails.json`; the canonical package command above is authoritative.
- Packaged native/real-backend smoke: PASS. The signed `.app` loaded a temporary demo session, started a broadcast, and served two independent Chromium player documents. The first controller moved the shared selection to `ДЕНЬ ВОЗРОЖДЕНИЯ`, and the observer followed. In the real master window, the observer was made controller; the shared selection was preserved, the former controller became inert, and the replacement moved both views to `ДИРЕКТИВЫ VAULT-TEC`. Reloading the former controller retained its logical identity, observer role, and authoritative selection. Final diagnostic: `PASS native BUG-008: ДЕНЬ ВОЗРОЖДЕНИЯ preserved across role swap; ДИРЕКТИВЫ VAULT-TEC controlled by replacement; former controller reconnected as observer`.
- `git diff --check`: PASS.

| Criterion | BUG-008 evidence |
|---|---|
| SC-034 | `tests/browser/player-sessions-control.spec.mjs` sends 100 observer pointer/focus/navigation/paging/back/activation/hacking-preview attempts and asserts no local semantic-selection change, no outbound presentation/gameplay request, and no canonical revision change. Accepted controller movements reach every assigned view in revision order. |
| SC-035 | `internal/control/service_test.go` exercises 100 presentation-action/reassignment interleavings with processing-time authorization. Browser role-swap coverage proves the promoted observer is immediately interactive from the shared view and the demoted controller is immediately inert and follows the new controller. |
| SC-036 | Browser late-join, new-tab, reload, and reconnect cases render the current authoritative selection/page/preview from the first complete snapshot, with no default-selection flash or local observer reset. |

## BUG-009 controller presentation-latency regression

- The red production-composed regression delayed generated ConnectRPC `SetPresentation` and reproduced `.shared-input-pending`, `cursor: not-allowed`, reduced opacity, and desaturation on an active controller while a newer presentation request remained eligible. The asset-contract regression simultaneously identified the combined blocking expression containing `pendingPresentationAction`.
- `renderPlayerContext()` now derives blocking shared-input styling only from pending shared gameplay, command, or terminal-navigation work. Presentation request correlation, accepted-revision completion, stale-result suppression, latest-wins replacement, and authoritative-only semantic selection remain unchanged. Observer read-only styling and genuine gameplay/navigation pending styling remain locked.
- `tests/browser/player-sessions-control.spec.mjs` delays the first presentation request, performs 100 pointer plus 100 keyboard movement attempts, observes every `#screen` class mutation, dispatches a newer eligible target, and proves zero transient blocking class, normal pointer/opacity/saturation, no optimistic selection, and authoritative latest-target convergence. The paired observer and delayed-`Navigate` controls retain `aria-readonly`, `not-allowed`, reduced-opacity, and desaturated behavior.
- Focused BUG-009/BUG-008 browser coverage passes 23/23 applicable journeys with two real-ngrok cases correctly skipped. The final complete Playwright run passes 126/126 applicable journeys with the same two opt-in ngrok cases skipped, covering controller/observer presentation, role swap, reconnect, audio, responsive layout, state-changing commands, persistence, and private-player boundaries. A pre-existing immediate geometry assertion exposed a one-frame CRT layout race during the first full runs; polling the unchanged containment predicate passes all three viewports twice (6/6) and the final full run.
- `make fmt-check`, `go vet ./...`, the full `go test ./... -count=1`, and the full `go test -race ./...` suite pass with `FALLOUT_NGROK_*` overrides unset; the networked suites ran outside the restricted sandbox so `httptest` could bind loopback ports. `scripts/proto-check.sh`, `scripts/proto-breaking.sh --all-fixtures`, and `scripts/wails-bindings-check.sh` pass at schema revision `89e5f5a7d0aadc0f5f62a5825787ae15e77e524f6cead55aa3ff15eaf7aefaa3`. The aggregate `make check` dependency graph now uses the current `deps-frontend` target for protobuf preparation and passes end to end.
- `npm --prefix frontend run build`: PASS for the client and Overseer bundles. `make package`: PASS for locked dependencies, protobuf verification, generated client, Wails bindings, both frontends, dependency/license inventory, clean Darwin/arm64 compilation, packaging, and ad-hoc signing. Output: `build/bin/Fallout Terminal.app`.
- Packaged native/real-backend smoke: PASS. The signed `.app` loaded a temporary demo session and served independent controller and observer Chromium documents through the production ConnectRPC server. While two `SetPresentation` requests were held, 100 repeated pointer/keyboard movements produced zero false-lock transitions, normal controller styling, no optimistic selection, and authoritative latest-wins convergence to `АРХИВ СМОТРИТЕЛЯ`; the observer remained visibly read-only and converged. A separately delayed generated `Navigate` retained the locked cursor, reduced opacity, and desaturation until its authoritative result, then both documents converged. Final diagnostic: `PASS native BUG-009: 100 pointer+keyboard repeats had zero false locks; АРХИВ СМОТРИТЕЛЯ won authoritatively; delayed Navigate retained locked styling; observer stayed read-only and converged`.
- `git diff --check`: PASS.

| Criterion | BUG-009 evidence |
|---|---|
| SC-037 | Production-composed and packaged real-backend journeys each execute at least 100 controller pointer and keyboard presentation movements under delayed `SetPresentation`, observe zero `.shared-input-pending` transitions and zero grey/locked controller styling, accept a newer movement, retain authoritative-only selection, and converge the controller plus observer on the latest accepted target. Delayed navigation and observer controls remain visibly and behaviorally locked. |

## BUG-010 bounded hacking-hover presentation regression

- The red production-composed regression held the first generated ConnectRPC `SetPresentation` while rapidly traversing 100 hacking filler targets and a final password target. Before the correction it observed 101 presentation requests queued behind the first request and stale intermediate replay.
- The browser now retains one correlated in-flight presentation plus one replaceable latest desired semantic target. Duplicate and superseded unsent targets are suppressed; the latest target drains only after authoritative completion or rejection and is revalidated against controller role, broadcast, terminal, and presentation context before dispatch.
- Focused presentation coverage passes 9/9 cases, including controller latency styling, reassignment, queued-intent invalidation, navigation-context replacement, transport-failure recovery, responsive presentation states, and rapid hacking hover. The rapid case proves one blocked request, at most one final follow-up, controller/observer convergence, and no intermediate highlight, preview, reveal, or `single`/`multiple` cue replay.
- `make fmt-check`, `git diff --check`, `go vet ./...`, `GOCACHE=/private/tmp/fallout-bug010-go-cache go test ./... -count=1`, and `GOCACHE=/private/tmp/fallout-bug010-race-cache go test -race ./... -count=1`: PASS. The linker emitted only the existing macOS deployment-target warnings.
- `scripts/proto-check.sh`, `scripts/proto-breaking.sh --all-fixtures`, and `scripts/wails-bindings-check.sh`: PASS at schema revision `89e5f5a7d0aadc0f5f62a5825787ae15e77e524f6cead55aa3ff15eaf7aefaa3`; Wails exposes exactly 34 accepted desktop methods.
- `npm --prefix frontend run build`: PASS. `npm --prefix tests/browser test`: PASS — 130/130 applicable journeys, with two real-ngrok cases correctly skipped as opt-in.
- `make package`: PASS for locked dependencies, protobuf verification, generated clients and Wails bindings, both frontends, dependency/license inventory, clean Darwin/arm64 compilation, packaging, and ad-hoc signing. Output: `build/bin/Fallout Terminal.app`.
- Packaged native/real-backend smoke: PASS. The signed app loaded a temporary demo session and activated the hacking terminal for independent controller and observer Chromium documents. While the first `SetPresentation` was held, 100 rapid hacking targets produced one in-flight request; release produced exactly one final follow-up. Both views converged on the final password target, and the controller recorded exactly two applicable preview cues with no stale highlight, preview, reveal, or audio replay. Final diagnostic: `PASS native BUG-010: 100 rapid hacking targets held at one in-flight request; 2 total presentation request(s); controller/observer converged; 2 preview cue(s), no stale replay`.

| Criterion | BUG-010 evidence |
|---|---|
| SC-038 | Production-composed and packaged real-backend journeys each hold the first presentation request during at least 100 rapid hacking-target transitions, observe one in-flight request plus no more than one latest follow-up, converge active and observer views on the final target, and record no superseded highlight, reveal, preview, or audio replay. Full Go/race/vet, frontend, 130-case browser, protocol/binding, package, observer, gameplay-pending, responsive-layout, persistence, and private-operation gates remain green. |
