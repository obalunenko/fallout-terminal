# Bug Report: T048 cannot turn the update RED test GREEN within its file inventory

**Type**: Spec conflict
**Severity**: High
**Feature**: 033-frontend-vue-typescript-migration
**Status**: Patched
**Reported**: 2026-08-30
**Patched**: 2026-08-30

## Description

T048 requires the focused assertion `Vue update offer preserves cumulative revision and focus` to pass, but its modifiable file inventory excludes `frontend/overseer/src/App.vue`, `frontend/overseer/src/mount.ts`, and `frontend/overseer/src/index.html`. The existing `App.vue` intentionally renders no leaf, so the new application-update components and composable cannot be mounted or receive the typed coexistence bridge. The legacy document also retains the same update IDs, so mounting Vue without removing that markup would create mixed ownership and duplicate IDs. Implementing the required GREEN state would therefore require undeclared file changes.

## Artifact Traceability

### spec.md

- **Affected user story**: User Story 2 — The Overseer retains every privileged control.
- **Affected requirements**: FR-027 and FR-031.
- **Gap identified**: T048's local completion scope does not include the existing Vue root integration files required to make update presentation observable.

### plan.md

- **Affected section**: Wave d — Overseer leaf components and composables.
- **Impact**: The plan requires each implementation slice to move a complete subtree and pass its focused test before the next slice, which requires task-local root integration.

### tasks.md

- **Affected task**: T048.
- **False completions**: None; T048 remains open.
- **Missing scope**: Add `frontend/overseer/src/App.vue`, `frontend/overseer/src/mount.ts`, and `frontend/overseer/src/index.html` to T048's modifiable files.

## Root Cause Analysis

Task generation separated leaf construction from later family-join work, but also declared T048—not T069—as the GREEN owner for T047. That made T048's verification contract inconsistent with its file inventory.

## Recommended Fix

1. Patch T048's file inventory to include the existing Vue root, mount integration, and production document files.
2. Preserve T069 as the later family-level single-owner join/review gate.
3. Verify the cross-artifact patch, then resume T048.
