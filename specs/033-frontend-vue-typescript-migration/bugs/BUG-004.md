# Bug Report: T052 cannot mount or exercise the command-approval leaf

**Type**: Spec conflict
**Severity**: High
**Feature**: 033-frontend-vue-typescript-migration
**Status**: Patched
**Reported**: 2026-08-30
**Patched**: 2026-08-30

## Description

T052 must turn T051 GREEN and remove the legacy command-approval owner, but its file inventory excludes `App.vue`, the governed candidate coexistence entry, and the focused browser spec. Without those files the new component cannot be mounted into `#overseerVueLeaves`, and the raw Go fixture cannot compile the SFC graph.

## Artifact Traceability

### spec.md

- **Affected user story**: User Story 2 — The Overseer retains every privileged control.
- **Affected requirements**: FR-022, FR-031, and FR-032.
- **Gap identified**: The declared GREEN task cannot establish or test its single Vue owner locally.

### plan.md

- **Affected section**: Wave d — Overseer leaf components and composables.
- **Impact**: The complete command-approval slice requires the existing root and candidate injection seam.

### tasks.md

- **Affected task**: T052.
- **False completions**: None; T052 remains open.
- **Missing scope**: Add `frontend/overseer/src/App.vue`, `frontend/overseer/test-fixtures/candidate-main.ts`, and `tests/browser/state-changing-command-approval.spec.mjs` to T052's modifiable inventory.

## Root Cause Analysis

Task generation deferred family joins to T070 while also assigning each implementation slice an immediate focused GREEN contract. T052 therefore omitted files that its local completion rule requires.

## Recommended Fix

Add the existing task-local mount/test seam to T052 and preserve T070 as the later family-wide ownership review gate.
