# Bug Report: T053 cannot mount the terminal-navigation approval leaf

**Type**: Spec conflict
**Severity**: High
**Feature**: 033-frontend-vue-typescript-migration
**Status**: Patched
**Reported**: 2026-08-30
**Patched**: 2026-08-30

## Description

T053 must migrate the terminal-navigation approval UI, remove its legacy owner, and pass an immediate focused browser assertion. Its file inventory excludes `App.vue` and the governed candidate coexistence entry, so the new component cannot be mounted into `#overseerVueLeaves` for that GREEN test. Adding the second legitimate coordination subscriber also invalidates T052's regression assertion that hard-codes one aggregate `coordination-state` release instead of checking that a repeated app unmount adds no releases.

## Artifact Traceability

### spec.md

- **Affected user story**: User Story 2 — The Overseer retains every privileged control.
- **Affected requirements**: FR-002, FR-004, FR-022, FR-028, and FR-050.
- **Gap identified**: The declared GREEN task cannot establish or exercise its single Vue owner locally.

### plan.md

- **Affected section**: Wave d — Overseer leaf components and composables.
- **Impact**: The complete terminal-navigation slice requires the existing application root and candidate injection seam.

### tasks.md

- **Affected task**: T053.
- **False completions**: None; T053 remains open.
- **Missing scope**: Add `frontend/overseer/src/App.vue`, `frontend/overseer/test-fixtures/candidate-main.ts`, and `tests/browser/state-changing-command-approval.spec.mjs` to T053's modifiable inventory.

## Root Cause Analysis

Task generation assigned each leaf an immediate candidate-browser GREEN contract while deferring family review to T070, but omitted the shared integration files from this leaf's otherwise complete inventory.

## Recommended Fix

Add the existing task-local mount seam and the affected aggregate-cleanup regression to T053, and preserve T070 as the later family-wide ownership review gate.
