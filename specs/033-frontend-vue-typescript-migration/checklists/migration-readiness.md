# Migration-Plan Readiness Checklist: Vue and TypeScript Frontend Migration

**Purpose**: Formal reviewer gate for the quality, clarity, consistency, measurability, and implementation readiness of the feature 033 migration requirements and planning artifacts
**Created**: 2026-08-30
**Feature**: [spec.md](../spec.md)
**Inputs assessed**: [plan.md](../plan.md), [research.md](../research.md), [quickstart.md](../quickstart.md), [migration-ownership.md](../migration-ownership.md), [frontend-build-generation.md](../contracts/frontend-build-generation.md), and [trust-and-evidence-boundaries.md](../contracts/trust-and-evidence-boundaries.md)
**Authority**: Clarified feature specification and Fallout Terminal Constitution 9.0.0
**Depth / actor / timing**: Formal, architecture-and-release-critical review by plan authors and PR reviewers before task generation or implementation

**Assessment legend**: `[x]` means the written requirements are ready; `[ ]` means the cited artifact or requirement needs clarification or added planning coverage. This assesses the English requirements and plan, not implementation behavior.

## Requirement Completeness

- [x] CHK001 Are the single `frontend/` npm workspace, sole `frontend/package-lock.json`, sole clean-install workflow, two application manifests, and prohibition on app-local lockfiles or install paths all specified together? [Completeness, Spec §§FR-011, FR-038; Plan §Project Structure; Build Contract §Workspace ownership]
- [x] CHK002 Are mutually compatible exact pins and ownership recorded for `vue` `3.5.42`, `typescript` `6.0.3`, `@vitejs/plugin-vue` `6.0.8`, and `vue-tsc` `3.3.11`, including fixture-based compatibility evidence and rejected alternatives? [Completeness, Spec §§FR-041–FR-042; Research §Exact Vue and TypeScript dependency pins]
- [x] CHK003 Are the current exact Wails, ConnectRPC, protobuf, Vite, Node.js, and Playwright pins identified as immutable, with unrelated dependency upgrades expressly excluded? [Completeness, Spec §§FR-024, FR-042; Plan §Technical Context; Build Contract §Workspace ownership]
- [x] CHK004 Are the Overseer and Player roots, HTML and TypeScript entrypoints, Vite configurations, outputs, adapters, embedding consumers, and trust domains defined independently? [Completeness, Spec §§FR-001, FR-005, FR-029; Research §Vue roots, entrypoints, and Vite integration; Build Contract §Independent Vue builds]
- [x] CHK005 Does the Player boundary cover direct and transitive dependency paths to Overseer source/state, Wails bindings, `@wailsio/runtime`, native/filesystem capabilities, private types, and a shared cross-boundary store? [Coverage, Spec §§FR-005, FR-025, FR-037; Plan §Architecture and Trust Boundaries; Trust Contract §Application dependency boundaries]
- [x] CHK006 Are all eight ConnectRPC methods and their names, paths, messages, descriptors, field numbers, binary wire encoding, cardinalities, Connect errors, typed action results, limits, authorization, streaming, revision ordering, reconnect, backpressure, cancellation, unary fallback, and public/private boundaries explicitly preserved, with no new operational RPC? [Completeness, Spec §§FR-017–FR-020, FR-024, FR-049; Trust Contract §Preserved Player RPC contract; Build Contract §Public browser protobuf generation]
- [x] CHK007 Is the browser generator change bounded to deterministic checked-in `protoc-gen-es` `target=ts` output with `.js` import specifiers, exact provenance and inventory, strict compilation, drift checks, and no parallel generated or transpiled JavaScript tree? [Completeness, Spec §§FR-012–FR-013, FR-044, FR-049; Research §`protoc-gen-es` `target=ts` output and imports; Build Contract §Public browser protobuf generation]
- [x] CHK008 Are Wails bindings explicitly generator-owned and unedited in supported JavaScript, JSDoc, and declaration form, exempt only from applicable handwritten-source scans, and consumable solely through the typed validating desktop adapter? [Completeness, Spec §§FR-013–FR-015; Research §Wails JavaScript, JSDoc, and declaration consumption; Build Contract §Wails binding generation and typed consumption]
- [x] CHK009 Are one shared strict TypeScript base, two application-specific configurations, strict SFC/template checks, `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, and final removal of `allowJs`/`checkJs` all documented? [Completeness, Spec §§FR-001, FR-009, FR-016; Research §Strict TypeScript configuration; Build Contract §Strict compiler policy]
- [x] CHK010 Are runtime-validation obligations defined for localStorage/storage events, Wails events/results, Connect-decoded semantic values, DOM/forms/pointer/keyboard input, clipboard, sound assets, presentation streaming, and other untrusted external values without transferring semantic validation or authorization away from existing adapters and Go services? [Coverage, Spec §FR-015 and SC-012; Trust Contract §External runtime validation]
- [x] CHK011 Do the component/composable maps assign every required Overseer and Player area to named Vue components and focused composables or adapters, while limiting state to typed props/emits and `ref`, `reactive`, and `computed`? [Completeness, Spec §§FR-003–FR-004, FR-008; Plan §Component and Composable Map]
- [x] CHK012 Are all approved imperative seams assigned a Vue owner with acquisition and cleanup rules for timers, animation frames, subscriptions, audio nodes, AbortControllers, streams/iterators, observers, temporary nodes, and document/window listeners, and must `$speckit-tasks` assign direct implementation/cleanup task IDs rather than only a final audit? [Coverage, Spec §§FR-007, FR-050; Plan §§Task-Generation Execution Contract, Imperative Browser Ownership and Cleanup; Trust Contract §§Task-local evidence and RED/GREEN contract, Imperative Vue-owned seams and cleanup]
- [x] CHK013 Does the producer-consumer map cover workspace manifests/lockfile, Vite/TypeScript/HTML/entrypoints, CSS/fonts/sounds/assets, protobuf generation, Wails generation, scripts, Taskfile, CI, browser fixtures/selectors, native embedding, buildtool, package checks, documentation, and active Speckit governance/templates? [Completeness, Spec §§FR-029, FR-030, FR-034–FR-035; Plan §Producer and Consumer Impact Map; Build Contract §Producer and consumer update matrix]
- [x] CHK014 Are final forbidden states and path-exact exclusions specified for handwritten production `.js`, legacy bootstrap, candidate/staging entries, temporary mounts/switches, mixed DOM ownership, `allowJs`/`checkJs`, broad `any`, `@ts-nocheck`, blanket assertions, and unexplained suppressions? [Completeness, Spec §§FR-010, FR-016, FR-039 and SC-002; Trust Contract §Final forbidden-state boundary]

## Requirement Clarity

- [x] CHK015 Are the canonical Taskfile target names for workspace and per-application `vue-tsc` checks and both independent builds specified as clearly as the underlying npm commands, so final validation cannot form an undocumented direct-command workflow? [Clarity, Spec §§FR-009, FR-034–FR-035, FR-038; Plan §§Canonical Frontend Task Contract, Verification Plan; Build Contract §§Canonical Taskfile commands, Canonical workflow integration]
  - Resolution: The plan, build contract, and quickstart now name all ten canonical `frontend:*` targets, assign exact install/type/build/compatibility/boundary/policy/reproducibility ownership, and treat npm commands only as Task-invoked implementation details. FR-009, FR-034, FR-035, and FR-038 are fully covered.
- [x] CHK016 Are the final root selectors, source entrypoints, Vite configs, output directories, asset roles, and embedding consumers concrete enough to distinguish the two build products without relying on implied repository conventions? [Clarity, Spec §§FR-005, FR-029; Research §Vue roots, entrypoints, and Vite integration; Build Contract §Independent Vue builds]
- [x] CHK017 Is preserved RPC behavior expressed through an exact method/cardinality inventory plus global path, encoding, limit, error, authorization, and capability rules, and must `$speckit-tasks` assign direct generator, transport, contract, browser, and Go-verification task IDs rather than a “same as FR-032” or umbrella mapping? [Clarity, Spec §§FR-017–FR-021; Plan §Task-Generation Execution Contract; Trust Contract §Preserved Player RPC contract]
- [x] CHK018 Is declaration ownership unambiguously limited to the shared capability-neutral compiler policy plus declarations inside each owning application, with no authored cross-application type module or type-only boundary exception? [Clarity, Spec §§FR-005, FR-045; Plan §§Architecture and Trust Boundaries, Wave c; Research §§Strict TypeScript configuration, Vue roots, entrypoints, and Vite integration]
  - Resolution: The previous declaration-scope ambiguity is removed. All artifacts now permit sharing only the npm install/lock boundary, exact tooling, and `frontend/tsconfig.base.json`; every authored declaration remains application-owned, and type-only imports cannot create a cross-boundary contract.
- [x] CHK019 Is strict Vue checking tied to concrete `vue-tsc --noEmit` commands, strict compiler flags, application include boundaries, generated Player TypeScript, and final prohibition of JavaScript fallback configurations? [Clarity, Spec §§FR-009, FR-016, FR-034; Research §Strict TypeScript configuration; Build Contract §§Canonical Taskfile commands, Strict compiler policy]
- [x] CHK020 Is the reproducibility decision objective about comparison scope—two builds of one revision and lockfile—while correctly distinguishing historical legacy digests from future Vue bundle values? [Clarity, Spec §SC-004; Research §Vite byte reproducibility; Build Contract §Independent Vue builds]
- [x] CHK021 Do deterministic checks require actionable file/artifact identification and positive/negative self-tests rather than merely stating that drift or forbidden state must fail? [Measurability, Spec §FR-040; Build Contract §Public browser protobuf generation; Trust Contract §Final forbidden-state boundary]
- [x] CHK022 Does every temporary-mechanism row distinguish temporary production compatibility from durable test infrastructure and give the exact owning file, selector/root/entry/config, creation task, owner, permitted scope, expiry wave, unconditional removal task, and executable absence check? [Clarity, Spec §§FR-031–FR-033 and SC-011; Plan §Rollback, Compatibility, and Final Removal; Ownership Record §§Temporary mechanism register, Retained browser evidence and test infrastructure]
  - Resolution: Exact rows now cover both legacy tsconfigs, both candidate documents and entries, both Vite and Playwright selections, staging/leaf roots, bridges/callbacks, legacy script tags, legacy lifecycle resources, and the deliberate protobuf drift mutation. The typed fake `DesktopPort` remains separately governed permanent test-only evidence.

## Requirement Consistency

- [x] CHK023 Are the specification, plan, research, and contracts consistent that this migration changes presentation/source ownership only and introduces no product behavior, persistent/canonical model, protobuf schema, Go API, RPC, authentication, tunnel, update-policy, or security-boundary change? [Consistency, Spec §§FR-023–FR-028, FR-037, Out of Scope; Plan §§Technical Context, Architecture and Trust Boundaries; Research §Model and contract artifacts]
- [x] CHK024 Is the clean-verification Node.js instruction consistent with exact Node.js `26.8.1`, and must `$speckit-tasks` assign direct wave-a producer/self-test task IDs proving `26.8.1` passes while an older and a newer version fail? [Consistency, Spec §FR-042 and Assumptions; Plan §§Technical Context, Task-Generation Execution Contract; Build Contract §Exact Node runtime contract; Quickstart §1]
  - Resolution: Wave-a guidance changes `node:check` to equality, adds deterministic positive/negative self-tests, and requires agreement across `.nvmrc`, `Taskfile.yml`, both CI workflows, three manifests, the lockfile, quickstart, README, and contributor documentation.
- [x] CHK025 Are generation requirements consistent that only browser source representation changes while schemas, descriptors, fields, Go output, paths, wire behavior, service behavior, and capability boundaries remain untouched? [Consistency, Spec §§FR-012, FR-017, FR-024, FR-049; Research §`protoc-gen-es` `target=ts` output and imports; Ownership Record §Wave b]
- [x] CHK026 Are Wails requirements consistent that generated JavaScript remains both exempt from handwritten-source removal and included in generation, inventory, integrity, dependency-boundary, and package checks? [Consistency, Spec §§FR-010, FR-013–FR-014; Build Contract §Wails binding generation and typed consumption; Trust Contract §Final forbidden-state boundary]
- [x] CHK027 Are browser/visual fixtures consistently limited to DOM/application parity evidence while Wails integrity, native embedding/startup, resources, clipboard/dialog/secure-store behavior, and packages retain separate evidence classes? [Consistency, Spec §FR-030; Research §Browser fixture integration; Quickstart §§8, 10, 12; Trust Contract §Browser versus native evidence]
- [x] CHK028 Is the mandatory a–i order consistent across the plan and ownership record, including the exhaustive capability-neutral Player foundation exception in wave c, prohibition of Player feature/business/production work before the complete wave-e exit, atomic Player cutover in h, and final cleanup only in i? [Consistency, Spec §§FR-043–FR-048; Plan §§Task-Generation Execution Contract, Implementation Waves; Ownership Record §§Waves a–i]
- [x] CHK029 Are selectors, `.mjs` tests, snapshots, CSS, fonts, sounds, copy, semantics/accessibility, focus, keyboard, pointer, timing, geometry, and gesture/audio baselines consistently immutable, with no planned Vue-accommodation updates? [Consistency, Spec §§FR-002, FR-030 and SC-001; Plan §§Component and Composable Map, Implementation Waves; Ownership Record §Rules that apply to every wave]

## Acceptance Criteria Quality

- [x] CHK030 Can SC-001 through SC-003 be objectively assessed through direct task IDs whose implementation tasks each have a locally executable check, followed by separate unchanged browser/visual, path-exact forbidden-state, clean-install, and workspace/per-application strict integration gates? [Measurability, Spec §§SC-001–SC-003; Plan §§Task-Generation Execution Contract, Verification Plan; Quickstart §§2–8]
- [x] CHK031 Does SC-004 have a researched, objective byte-reproducibility rule for both Vite trees plus deterministic generated sources, unchanged lockfile, and equivalent asset manifests/inventories? [Measurability, Spec §SC-004; Research §Vite byte reproducibility; Quickstart §§4–5]
- [x] CHK032 Do SC-005 and SC-006 require direct task IDs for generator/contract integrity and each independently testable RPC, revision, reconnect, pending-action, slow-stream, multi-tab, stale-effect, cancellation, fallback, and server-authority behavior, with local checks before shared integration? [Coverage, Spec §§SC-005–SC-006; Plan §Task-Generation Execution Contract; Trust Contract §§Task-local evidence and RED/GREEN contract, Preserved Player RPC contract, Preserved state and lifecycle rules; Ownership Record §§Waves b, f–h]
- [x] CHK033 Does SC-007 have an explicit implementation wave and named verification surface for round-tripping representative current and legacy session and player-configuration fixtures, including compatible unknown fields? [Coverage, Spec §§FR-023, SC-007; Plan §§Canonical Frontend Task Contract, Waves e and i, Verification Plan; Quickstart §6]
  - Resolution: `task frontend:compatibility:check` now owns the production-fidelity current/legacy session and player-configuration fixture set in waves e and i, including open/edit/save/reopen comparison of fields, defaults, references, unknown fields, location, and business meaning. Task generation must record reviewed fixture paths and reuse existing version-1 codecs.
- [x] CHK034 Do SC-008 through SC-011 have objective journey, package, repository-quality, and per-wave ownership/removal evidence with honest host limitations? [Coverage, Spec §§SC-008–SC-011; Plan §§Verification Plan, Rollback, Compatibility, and Final Removal; Quickstart §§6–12]
- [x] CHK035 Does SC-012 map every class in its complete invalid/valid fixture population—native events, storage, document input, network values, clipboard results, and command results—to a named wave and final governed test surface? [Coverage, Spec §§FR-015, SC-012; Plan §§Canonical Frontend Task Contract, Waves c/e/f/g/h/i, Verification Plan; Trust Contract §External runtime validation]
  - Resolution: `task frontend:boundary:check` now owns one reviewed manifest covering all required boundary classes, required entry metadata, wave-focused mappings, valid acceptance, invalid pre-mutation rejection, and failure on missing test mappings. Its completeness claim is explicitly limited to the reviewed manifest.
- [x] CHK036 Must `$speckit-tasks` map every FR-001–FR-050 and SC-001–SC-012 to one or more explicit valid task IDs, including a direct implementation or verification task rather than only a final umbrella audit, with no invalid ranges, “same as” references, or “every wave task” placeholders? [Traceability, Spec §§FR-001–FR-050, SC-001–SC-012; Plan §§Task-Generation Execution Contract, Implementation Waves, Verification Plan]
  - Resolution: The planning ledger names every implementation wave and verification surface; regenerated tasks must expand those rows into explicit IDs and mechanically validate every referenced ID before implementation.

## Scenario and Migration-Wave Coverage

- [x] CHK037 Does every wave’s DOM ownership record contain all six required fields and explicitly include focused type-check, build, browser, and visual-parity evidence, even when the wave primarily changes generation? [Coverage, Spec §§FR-031–FR-033 and SC-011; Ownership Record §§Waves a–i]
  - Resolution: Wave b now requires the unchanged Player build, focused ConnectRPC journeys, and existing CRT/rendering visual suite with immutable snapshots and zero selector, visual, CSS, copy, accessibility, layout, timing, or geometry baseline change; task generation must record the exact command and cannot approve snapshots.
- [x] CHK038 Are mount boundaries, adjacent legacy-owned subtrees, remaining legacy files/handlers, prohibited cross-query/cross-mutation boundaries, focused evidence, and removal criteria explicit for all nine waves, with exactly one owner per subtree? [Completeness, Spec §§FR-031–FR-033; Ownership Record §§Rules and Waves a–i]
- [x] CHK039 Must `$speckit-tasks` assign direct RED/GREEN implementation or verification task IDs to reconnect/overflow, tab races, malformed/stale values, unmount during each owned resource class, pointer/layout races, audio lock/failure, stale dialogs, legacy persistence, access modes, generation drift, ownership collision, stale packages, and source-control rollback instead of mapping this checklist item only to final review? [Coverage, Edge Cases, Spec §Edge Cases; Plan §§Task-Generation Execution Contract, Imperative Browser Ownership and Cleanup, Rollback, Compatibility, and Final Removal; Ownership Record §§Waves a–i]
- [x] CHK040 Are final Taskfile/CI/buildtool, embedding, resource, secret-boundary, package-content, active-documentation/template, rollback-reference, and truthful `NOT RUN` obligations assigned to exact files and direct task IDs without adding a runtime service, endpoint, native capability, browser privilege, or competing workflow, and does every Go-changing task cite the global per-commit `go fix ./...` rule? [Completeness, Spec §§FR-029, FR-034–FR-040; Plan §§Task-Generation Execution Contract, Producer and Consumer Impact Map, Verification Plan, Rollback; Quickstart §§9–12]

## Requirement-to-Plan Coverage Ledger

The groupings below account for every functional requirement and identify its primary implementation and verification surfaces. Every row is ready for task generation.

| Requirements | Primary implementation waves | Primary verification surfaces | Assessment |
|---|---|---|---|
| FR-001, FR-005, FR-008–FR-011, FR-016, FR-038, FR-041–FR-043 | a, c, e, h, i | `task frontend:build`, the three `frontend:typecheck*` targets, and `task frontend:policy:check` | Ready |
| FR-012–FR-014, FR-017, FR-024–FR-025, FR-040, FR-044, FR-049 | b, c, i | Generation/provenance/drift, strict generated compilation, RPC/schema/Go/Wails integrity and public-boundary checks | Ready |
| FR-004, FR-022, FR-026–FR-028, FR-045–FR-046 | c–e, i | Overseer focused/full browser and visual suites plus separate Wails/native/resource/secret/package gates | Ready |
| FR-023 | e, i | `task frontend:compatibility:check` with the reviewed current/legacy persistence fixture set | Ready |
| FR-003, FR-018–FR-021, FR-047 | f–h, i | Player candidate/production browser and visual suites, transport/authority/race/stream/lifecycle and dependency-boundary evidence | Ready |
| FR-015 | c, e, f, g, h, i | Wave-focused manifest subsets and final `task frontend:boundary:check` | Ready |
| FR-002, FR-006–FR-007, FR-029–FR-037, FR-039, FR-048, FR-050 | a–i | Per-wave ownership/parity/removal records and final browser, visual, build, native, resource, package, quality, and documentation gates | Ready |

| Success criterion | Planned wave(s) | Verification surface | Assessment |
|---|---|---|---|
| SC-001 | d–i | Focused and complete unchanged `.mjs`/visual suites | Ready |
| SC-002 | e, h, i | Overseer, Player, and final forbidden-state scans | Ready |
| SC-003 | a, c–i | `task frontend:build` clean install plus all three `frontend:typecheck*` targets | Ready |
| SC-004 | a, b, i | Lockfile invariance, two generations, two byte-identical Vite trees, asset manifests | Ready |
| SC-005 | b, c, h, i | RPC inventory/contract and Player dependency/capability scans | Ready |
| SC-006 | f–h | Reconnect/revision/action/slow-stream/multi-tab/authority stress journeys | Ready |
| SC-007 | e, i | `task frontend:compatibility:check` current/legacy session/player-config round trip | Ready |
| SC-008 | e, h, i | Local/LAN/public, credential, clipboard, tunnel, and boundary journeys | Ready |
| SC-009 | e, h, i | Matching-host package and resource/content inspection | Ready |
| SC-010 | b–i | Clean-checkout drift, bindings, embeds, packages, builds, and repository quality | Ready |
| SC-011 | a–i | Per-wave ownership/build/browser/visual record and final empty inventory | Ready |
| SC-012 | c, e, f, g, h, i | Wave-focused manifest subsets and final `task frontend:boundary:check` | Ready |

## Post-analysis task-generation gates

- CHK012, CHK017, CHK024, CHK030, CHK032, CHK036, and CHK039 each require direct regenerated task IDs; a final audit task cannot be their only mapping.
- Generated task `Files (modify/create/delete)` fields contain only complete repository-relative paths. `Read-only inputs` are separate. Bare basenames, directory inheritance, known-inventory globs, and prose shorthands are rejection conditions.
- Oversized equivalents of prior T047, T049, T053, T055, T062–T065, T083–T085, T095–T104, T112, T116, and T140 are split by one coherent UI, workflow, resource owner, or independently testable behavior. Integration and wave-exit tasks remain separate.
- The regenerated dependency graph is mechanically validated before implementation, including producer-before-consumer checks for all Taskfile targets, scripts, fixtures, generated files, RED/GREEN pairs, and the complete wave-e gate before Player feature work.
- `[P]` is optional and may appear only for disjoint exact files and evidence; correctness and safe join ordering take priority.

## Notes

- Reassessment completed 2026-08-30: all 40 requirement-quality decisions remain checked and `tasks.md` has been regenerated with the final executable-verification corrections. Readiness remains conditional on the final `$speckit-analyze`; implementation is forbidden until that analysis returns `READY` with zero blocking findings.
- No application code, generated file, approved architecture, product behavior, persistence contract, protobuf schema, Go API, RPC, authentication behavior, tunnel behavior, update policy, or security boundary is changed by this checklist.
- The immutable rollback commit `06696ee1c7155a1bb1135ef46ec91445dd73a2a4` exists and is an ancestor of the assessed planning revision.
- The retained resolution notes record planning-readiness findings and their disposition; they are not claims about application behavior.
